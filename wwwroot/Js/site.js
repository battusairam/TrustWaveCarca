
    // wwwroot/js/site.js
    window.setElementStyle = (element, styleProperty, value) => {
        element.style[styleProperty] = value;
    };

    window.scrollToBottom = (element) => {
        element.scrollTop = element.scrollHeight;
    };




